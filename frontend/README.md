# Frontend

React + Vite shell for the DICOM viewer workspace.

## Environment

Copy `.env.example` into `.env` if you want to point the UI at a different backend.

```bash
VITE_API_BASE_URL=http://127.0.0.1:8000
```

## Commands

```bash
npm install
npm run dev
```
